# Optional: absolute path to mounted volume (Railway). Example: /data
# DATA_DIR=/data

# Optional: where farm error screenshots go (default: ./artifacts)
# ARTIFACTS_DIR=/data/artifacts

# Set to 1 to enforce 24h per-account + global farm cooldown (default: off for easier testing)
# FARM_COOLDOWN_ENABLED=1
