# Railway will prefer the Dockerfile in this repo over Nixpacks auto-detection.
# This file is kept as a fallback reference only — the Dockerfile is the source
# of truth for the build environment.
#
# To use: ensure "Dockerfile" is selected as the build method in Railway's
# service settings, or Railway will auto-detect it from the Dockerfile present.
